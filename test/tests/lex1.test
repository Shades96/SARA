// This is a comment
f(a,b,c) {
    let x = 0;  // comment
    x = (x + 1) + 1;
    x = x + (1 + 1);
    x = !x;
    x = -x;
    x = x + x;
    x = x * x;
    x = x / x;
    x = x && x;
    x = x || x;
    x = x < x;
    x = x > x;
    x = x == x;
    if 0 {
        x = (x + 1) + 1;
        x = x[x + 1];
        if 0 {
            f(x,x,x);
            x = f(x,x,x);
            x = f(f(x,x,x),x,x);
        }
    }
    while 0 {}
    return x;
}
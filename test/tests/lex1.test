// This is a comment
f(a,b,c) {
    let x = 0;  // comment
    x = !x;
    x = -x;
    x = x + x;
    x = x * x;
    x = x / x;
    x = x && x;
    x = x || x;
    x = x < x;
    x = x > x;
    x = x == x;
    if 1 {
        x = x + 1;
    }
    while 0 {}
    return x;
}
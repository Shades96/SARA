// This is a comment
f(a,b,c) {
    let x = 0;  // comment
    x = f(x);
    let y = f(x);
    x = f(x,x,x);
    x = (x + 1) + 1;
    x = x + (1 + 1);
    x = !x;
    x = -x;
    x = x + x;
    x = x * x;
    x = x / x;
    x = x && x;
    x = x || x;
    x = x < x;
    x = x > x;
    x = x == x;
    if 0 {
        x = (x + 1) + 1;
        x = x[x + 1];
        if x > (0 + 1) {
            f(x,x,x);
            // x = f(f(x,x,x),x,x);
        }
    }
    while 0 {}
    return x;
}